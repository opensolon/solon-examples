solon.app:
  group: demo       #配置服务使用的默认组
  name: helloconsumer    #发现服务使用的应用名

solon.cloud.jedis:
  server: "localhost:6379"
  password: 123456
  event:
    db: 0 #默认为 0，可不配置
    maxTotaol: 200 #默认为 200，可不配
  lock:
    db: 1

server.port: 9001